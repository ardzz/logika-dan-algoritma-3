#include "stdio.h"

int main(void){
    float a, b;
    a = 30;
    b = 10;

    printf("a + b = %f", a + b);
    printf("a - b = %f", a - b);
    printf("a * b = %f", a * b);
    printf("a / b = %f", a / b);

    return 0;
}